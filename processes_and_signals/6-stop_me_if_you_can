#!/usr/bin/env bash
# Send termination signal to the '4-to_infinity_and_beyond' process
pid=$(ps -eo pid,cmd | grep "./4-to_infinity_and_beyond" | grep -v grep | awk '{print $1}')
[ -n "$pid" ] && echo > "/proc/$pid/fd/0"
