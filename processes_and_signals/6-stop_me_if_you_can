#!/usr/bin/env bash
# This script is displaying something
#!/bin/bash
pid=$(ps -eo pid,comm | grep "4-to_infinity_and_beyond" | grep -v grep | awk '{print $1}')
[ -n "$pid" ] && echo "quit" > /proc/$pid/fd/0
